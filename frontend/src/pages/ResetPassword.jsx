import React,{useState}from"react";import{useSearchParams,Link}from"react-router-dom";import{api}from"../services/api";export default function ResetPassword(){const[sp]=useSearchParams(),[msg,setMsg]=useState("");async function go(e){e.preventDefault();try{const r=await api("/auth/reset-password",{method:"POST",body:JSON.stringify({token:sp.get("token"),password:new FormData(e.currentTarget).get("password")})});setMsg(r.message)}catch(e){setMsg(e.message)}}return <section className="authWelcome"><form className="authCard" onSubmit={go}><div className="authFormInner"><Link className="logo" to="/home">OR<span>BIS</span></Link><small>ACCOUNT SECURITY</small><h2>Reset your password.</h2><label>New password<input name="password" type="password" minLength="6" required/></label><button className="primary full">Update password</button>{msg&&<p>{msg}</p>}<Link to="/login">Back to sign in</Link></div></form></section>}
