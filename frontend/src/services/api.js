const BASE=import.meta.env.VITE_API_URL||"http://localhost:5000/api";export async function api(path,options={}){const isForm=options.body instanceof FormData;const r=await fetch(BASE+path,{credentials:"include",...options,headers:{...(isForm?{}:{"Content-Type":"application/json"}),...(options.headers||{})}});const ct=r.headers.get("content-type")||"";const data=ct.includes("application/json")?await r.json().catch(()=>({})):await r.text();if(!r.ok)throw new Error(data?.message||data||"Request failed");return data}export const API_BASE=BASE;
